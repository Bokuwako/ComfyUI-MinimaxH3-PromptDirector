# 변경 기록

## v2 — 2026-09-11

초판 이후 쌓인 작업을 한 번에 올립니다. 새 노드 2개, 새 모듈 4개, 그리고
실제로 영상이 잘못 나오던 원인들을 고쳤습니다.

파이썬이 바뀌었으므로 **ComfyUI 재시작이 필요합니다.**

---

### 새 기능

**에셋 라이브러리** — `📚 MMH3 Asset Save` / `📚 MMH3 Asset Load`

배우·의상·장소·소품·레이아웃·목소리 여섯 종류를 이름으로 저장해 두고 다른
워크플로우에서 불러 씁니다.

| 파일 | 하는 일 |
|---|---|
| `mmh3/library.py` | 저장소. 역할별 우선순위(`fullbody_threeview` → `bust_threeview` → `asset_sheet` → `master`)로 파일을 고릅니다 |
| `nodes/assets.py` | 저장/불러오기 노드. 같은 이름으로 저장하면 파일이 덧붙고, 배치는 `key_00`, `key_01` 로 갈립니다 |
| `routes.py` | `/mmh3_library/list·image·update·delete·open_folder`. 경로 탈출 방어와 320px 썸네일 캐시 포함 |
| `web/mmh3_asset_panel.js` | 전체 화면 라이브러리 패널. 노드의 콤보 목록을 그 자리에서 갱신합니다 |

**테마 (장르)** — `mmh3/themes.py`, `mmh3/themes.json`

`styles.json` 에 섞여 있던 장르를 분리했습니다. **스타일이 매체를, 테마가
장르를** 정합니다 — "2D 애니" + "그라비아 화보" 는 애니 화보가 됩니다.
브리프 앞에 장르 이름 한 줄이 들어가고, 조명·구도·의상은 모델이 그 장르
지식대로 고릅니다.

**샷 연결 방식** — `shotcards.SHOT_LINK`

컷 뒤의 샷이 앞 샷과 어떤 관계인지 고릅니다. 예전에는 한 가지뿐이었습니다.

- `같은 순간 — 카메라만 바뀜` : 자세와 동작까지 앞 샷에서 그대로 물려받습니다
- `이어지는 동작` (기본) : 장소·인물·의상·조명은 같고 동작만 이어집니다
- `새 장면` : 장소와 조명을 이어받지 않습니다. 외형과 의상은 유지됩니다

**대사 길이 검사** — `validator.check_dialogue_rate`

러닝타임에 대사가 안 들어가면 경고합니다. CJK 4.5자/초, 라틴 2.8단어/초 기준.
가이드라인의 "2.5–3 spoken words per second" 는 영어 단어 기준이라 일본어·
한국어에 그대로 쓰면 두 배로 틀립니다.

---

### 고친 것

**Prompt Freeze 편집 가이드라인을 다시 썼습니다 (11,349자 → 1,721자)**

편집 패스에서 `BASE_RULES` / `SPEAKER_RULES` 를 뺐습니다. 그 둘은 "처음부터
쓰는 법" 이라, 읽은 모델이 편집 대신 **새로 씁니다.** 머리말에 "이건 다시
쓰라는 초대가 아니다" 라는 변명 문단이 달려 있었던 게 그 증거였습니다 —
줘 놓고 무시하라고 하는 구조였습니다.

같이 걷어낸 것:

- **예시 문장 전부.** 가이드라인에 든 예시는 출력에 그대로 복사돼 나옵니다.
- **금지문.** `not improving, not tightening, not rephrasing, not summarising`
  처럼 하지 말라는 말을 늘어놓으면 약하게 먹거나 반대로 갑니다.

형식을 지키게 하는 가장 센 신호는 규격이 아니라 **눈앞의 프롬프트 자체**
입니다. 타임스탬프도 컷 문구도 화자 표기도 거기 이미 다 쓰여 있습니다.

더해서:

- 입력 길이에 맞춘 동적 컨텍스트 (4096–32768, 1024 단위)
- 가드 2종 — 출력이 입력과 **완전히 같을 때**, 그리고 **샷 본문은 그대로인데
  summary/retention 만 바뀌었을 때** 경고합니다

**비전 패스에서 물어볼 항목을 역할별로 줄였습니다** — `vision.ROLE_HEADINGS`

배경 사진에 `PEOPLE` / `CLOTHING` / `GAZE` / `EXPRESSION` / `PHYSIQUE` / `BODY`
를 물으면 여섯 줄이 전부 `not visible` 로 돌아오고, 그 인벤토리가 작성 패스로
넘어가 컨텍스트를 밀어냅니다. 역할이 정해진 그림은 물어볼 것만 묻습니다.
역할이 비어 있으면 예전처럼 전체를 묻습니다.

**컷마다 배경이 바뀌던 원인**

시스템 프롬프트의 `CONTINUITY` 블록은 "브리프가 준 것을 옮겨라" 라고만 하고,
"**샷 1에서 네가 지어낸 것도 지켜라**" 라는 말이 어디에도 없었습니다. 브리프가
장소를 안 주면 모델은 샷 1에서 자유롭게 창문을 만들고 샷 2에서 또 만듭니다 —
각 샷에서는 규칙을 어긴 적이 없습니다. 그 구속을 46,000자 시스템 프롬프트
안이 아니라 **샷 내용 바로 옆**에 답니다.

**자세 어휘** — `mmh3/acts.py`

| 항목 | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|
| `정상위` | 다리 기하가 없어 바닥에 편 채로 그려짐 | `정상위 (다리 펴고)` / `정상위 (다리 올림)` 둘로 분리. 새 항목은 `legs raised and wrapped around {B}'s waist` |
| `prone` 엎드린 자세 | `downward and forward` 로 두 축을 동시 지정 → 모델이 수직만 집음 | `doggy` 와 같은 척추 축 기준 앞뒤로 통일 |
| `carry_front` 대면 안고 서서 | `{A}'s back is turned away from {B}` — 마주 본 자세에서 정보가 0인데 렌더링 지시로 읽혀 뒷모습을 유발 | 삭제 |
| `edge` 가장자리 걸치기 | 81단어, 하는 쪽이 30번째 단어에서야 등장 → 받는 쪽만 그려짐 | 61단어, 21번째로 |
| 삽입 체위 4종 | `chest to chest` 만으로는 상체만 보장 → 골반 사이가 벌어짐 | `hips to hips` 등 골반 접촉을 따로 명시 |

이번에 확인된 규칙 세 가지를 파일 주석으로 남겼습니다.

1. **아는 이름을 부르는 쪽이 기하를 받아쓰는 쪽보다 항상 낫다.** 다리 위치를
   절 다섯 개로 지정했더니 모델이 그중 하나만 집었습니다. 같은 뜻의
   `legs around {B}'s waist` 한 구절이 다섯 개를 전부 담습니다.
2. **접촉면은 하나씩 따로 말해야 한다.** 상체 접촉이 골반 접촉을 함의하지
   않습니다.
3. **상대가 늦게 등장하면 한 사람만 그려진다.** 첫 문장이 길어져 상대가 30번째
   단어로 밀리자 화면에서 사라졌습니다.

---

### 바뀐 것 · 호환성

- `dialogue_language` 가 자유 입력에서 드롭다운으로 바뀌었습니다
  (Korean / Japanese / Chinese / English). 저장된 워크플로우가 들고 있던
  문자열 값은 그대로 살아남습니다.
- `theme` 이 Shot Settings 에서 `style` 바로 아래로 올라왔습니다. 둘을 한 쌍으로
  읽기 때문입니다.
- `missionary` **키는 그대로 둡니다.** 저장된 샷카드와 Shot Builder 기본값에
  영향이 없습니다. 한글 라벨만 `정상위 (다리 펴고)` 로 바뀌었습니다.
- `docs/AI_BRIEFING.md` 추가 — 다른 AI 대화창에 붙여넣어 Shot Builder 입력을
  대신 설계하게 하는 문서입니다.

---

## v1

초판. 8개 노드와 예제 워크플로우.
